# 项目图示

## 一、架构总览

```mermaid
flowchart TD
    subgraph 设备层
        A["PLC/CNC/机器人/仪表"]
    end

    subgraph 边缘层
        B["边缘采集代理<br/>设备连接/数据读取/测点转换/质量标记"]
        R["本地规则引擎<br/>安全类+状态类规则实时判定"]
        S["SQLite Outbox<br/>缓存/重试/补传"]
        B --> R
        R --> S
    end

    subgraph 中心层
        C["中心接入服务"]
        D["Kafka事件总线"]
        G["监控服务"]
        H["告警服务"]
        I["分析服务"]
        J["报表服务"]
        D --> G
        D --> H
        D --> I
        D --> J
        K["中心规则引擎<br/>复杂关联规则+安全类校验"]
        E["运维人员"]
    end

    subgraph 配置面
        F["统一配置管理<br/>编辑/校验/版本/审批/灰度"]
        F1["采集规则<br/>测点/频率/协议映射/状态策略"]
        F2["告警规则<br/>安全类/状态类/复杂关联"]
        F --> F1
        F --> F2
    end

    A -->|OPC UA/Modbus/私有协议| B
    R -->|声光报警/紧急保护| A
    S -->|MQTT上报<br/>测点数据+规则判定结果| C
    C --> D
    G -->|异常通知| E
    H -->|告警通知| E
    K -->|告警通知| E
    E -->|编辑规则| F
    F1 -->|configVersion 采集规则下发| B
    F2 -->|ruleVersion 告警规则下发| R
    F2 -->|ruleVersion 告警规则下发| K
    G -->|运行反馈| F
    H -->|运行反馈| F
    K -->|审计追溯| F
```

> 设备层 + 边缘层 + 中心层构成**数据面**，配置面即**控制面**。

## 二、中心层数据流与存储架构

```mermaid
flowchart TD
    subgraph 数据接入
        A["边缘节点 ×24"] -->|MQTT| B["中心接入服务<br/>协议解析/基础校验/路由分发"]
        B --> C1["Kafka<br/>device.telemetry.raw"]
    end

    subgraph 实时链路["实时链路（Flink + Kafka Streams）"]
        C1 --> E["Flink 实时 ETL<br/>去重/标准化/质量标记/测点映射"]
        E --> C2["Kafka<br/>device.telemetry.clean"]
        C2 --> D["Kafka Streams<br/>设备状态更新/窗口聚合"]
        C2 --> F["Flink CEP<br/>多条件组合异常检测"]
    end

    D -->|最新状态| P["Redis<br/>实时刷新 · ms级 · 最新1条"]
    D -->|窗口聚合| N["InfluxDB<br/>看现在 · 时序热数据/降采样"]
    C1 -->|原始数据归档| G["HDFS ODS<br/>Iceberg表 · 原始报文"]
    E -->|标准化明细| G3["HDFS DWD<br/>Iceberg表 · 清洗后明细"]
    E -->|告警索引| L["HBase<br/>服务翼 · 查得快"]
    F -->|告警事件| R["告警与工单"]

    subgraph 离线链路["离线链路（Spark + Airflow）"]
        G3 -->|Airflow 调度| G2["数据质量校验<br/>DWD完整性/记录数校验"]
        G2 --> H["Spark DWD 补充加工<br/>跨源关联/主数据补全"]
        H --> I["Spark DWS 聚合<br/>设备可用率/故障率/MTTR"]
        I --> J["Spark ADS 导出<br/>驾驶舱指标"]
    end

    subgraph 存储层["一湖两翼 + 全文检索"]
        K["HDFS + Iceberg<br/>数据湖底座 · 表格式管理"]
        L
        G
        G3
        M["Hive<br/>分析翼 · 算得清<br/>DWS汇总/ADS应用"]
        N
        O["MySQL<br/>驾驶舱指标/业务数据"]
        P
        ES["Elasticsearch<br/>搜得到 · 日志/告警全文检索"]
    end

    C2 -->|日志/告警事件| ES

    subgraph 服务层
        Q["监控大屏"]
        R
        S["驾驶舱/报表"]
        T["趋势分析"]
        U["预测性维护"]
    end

    H --> G3
    I --> M
    J --> O

    P --> Q
    L -->|历史查询 · 秒级 · 近N条| Q
    N --> T
    O --> S
    M --> S
    M --> U
    N -->|时序特征| U

    subgraph 调度与治理
        V["Airflow<br/>DAG调度/依赖管理/失败重跑"]
        W["实时质量监控<br/>Flink链路实时校验"]
        X["Hive Metastore<br/>统一数据目录/血缘追溯"]
    end

    V --> G2
    V --> H
    V --> I
    V --> J
    X <-->|双向依赖| M
    X --> H
```

> **数据流顺序**：边缘上报 → 中心接入（协议解析/基础校验）→ Kafka raw → Flink ETL（去重/标准化/质量标记）→ Kafka clean + HDFS DWD → 下游消费。ODS 层保存 Flink 处理前的原始报文，DWD 层保存 Flink 清洗后的标准化明细。
> **"一湖"**：HDFS + Iceberg 为湖仓一体底座，ODS 以 Iceberg 表格式保存原始报文（ACID 事务、Schema 演进、时间旅行），DWD 以 Iceberg 表格式保存清洗后明细（增量读取）。
> **"两翼"**：HBase（服务翼）提供毫秒级实时点查，Hive（分析翼）提供 SQL 批量分析。
> **Elasticsearch**：从 Kafka clean topic 消费日志和告警事件，仅承担全文检索职责（按关键词搜索错误信息、按 traceId 还原请求链路），日志的归档/点查/分析复用 HDFS/HBase/Hive，不重复建设。
> **Redis vs HBase**：Redis 缓存最新 1 条状态，大屏实时刷新用；HBase 存近 N 条历史，大屏详情查询用。
> **数据质量**：实时链路有 Flink 实时质量监控，离线链路在 ODS 写入后做第一道校验，Airflow 末端做最终校验。
> **数据分层**：ODS → DWD → DWS → ADS，详见 [06_数据架构与大数据处理.md](技术到场景/06_数据架构与大数据处理.md) 2.1 节。

## 三、可观测性

```mermaid
flowchart LR
    subgraph 数据源
        A1["设备层<br/>PLC/CNC/传感器"]
        A2["边缘节点 ×24"]
        A3["中心微服务"]
    end

    subgraph 指标采集
        B1["系统运维指标<br/>CPU/内存/QPS/错误率"]
        B2["node_exporter<br/>JMX_exporter<br/>应用Metrics_SDK"]
        B3["设备业务指标<br/>温度/振动/转速/OEE"]
        A2 & A3 --> B1 --> B2 -->|Pull| P["Prometheus"]
        A1 -->|传感器数据| B3
    end

    subgraph 边缘预处理["边缘预处理（日志/事件）"]
        E1["协议适配<br/>OPC-UA/Modbus→JSON"]
        E2["本地缓存+断点续传<br/>RocksDB·ACK确认"]
        E3["预过滤降噪<br/>心跳聚合·DEBUG采样"]
        E4["本地实时告警<br/>一级告警边缘直接触发"]
        E5["数据脱敏<br/>配方Hash·工号脱敏"]
        A1 --> E1 --> E2 --> E3 --> E4 & E5
    end

    subgraph 统一数据管道
        C1["MQTT"]
        C2["中心接入服务"]
        C3["Kafka raw"]
        C4["Flink ETL<br/>去重/标准化/质量标记"]
        C5["Kafka clean"]
        E3 -->|日志/告警/遥测| C1 --> C2 --> C3 --> C4 --> C5
    end

    subgraph 存储层
        I["InfluxDB<br/>时序存储"]
        ES["Elasticsearch<br/>全文检索"]
        H1["HDFS + Iceberg<br/>日志归档"]
        H2["HBase<br/>日志点查"]
        H3["Hive<br/>日志分析"]
    end

    C4 -->|设备业务指标| I
    P -->|Remote Write| I
    C5 -->|日志/告警事件| ES
    C5 -->|长期归档| H1
    ES --> H2 & H3

    subgraph 链路追踪
        D1["Jaeger"]
        D2["Context Propagation<br/>HTTP Header<br/>Kafka Header<br/>MQTT payload"]
        A2 & A3 --> D1
        D2 -.->|traceId全链路携带| D1
    end

    subgraph 统一观测平台
        G["Grafana"]
        G1["指标看板<br/>基础设施/中间件<br/>服务/业务四层"]
        G2["日志检索<br/>traceId关联"]
        G3["链路视图<br/>Span树分析"]
        G --> G1 & G2 & G3
    end

    subgraph 告警治理
        H["Alertmanager"]
        H0["收敛策略<br/>时间窗口聚合<br/>源头合并·根因识别"]
        H4["P0 电话+短信+企微"]
        H5["P1 短信+企微"]
        H6["P2 企业微信"]
        H --> H0 --> H4 & H5 & H6
    end

    P --> G1
    I --> G1
    ES --> G2
    D1 --> G3
    G1 & G2 & G3 -->|三支柱关联<br/>指标发现→链路定位→日志定因| G
    G -->|告警规则| H
    H4 & H5 & H6 --> K["工单服务<br/>自动化巡检/回滚"]
```

> **指标采集双链路**：系统运维指标走 Prometheus Pull（node_exporter / JMX / 应用 SDK），设备业务指标走统一数据管道（Flink），两者最终汇聚到 InfluxDB，由 Grafana 统一展示。
> **日志不走独立链路**：日志/告警事件和设备遥测数据统一走 边缘预处理 → MQTT → 中心接入服务 → Kafka → Flink ETL 管道，Elasticsearch 仅承担全文检索职责，归档/点查/分析复用 HDFS + HBase + Hive。
> **边缘网关五大职责**：协议适配、本地缓存+断点续传（RocksDB + ACK）、预过滤降噪、本地实时告警、数据脱敏。
> **三支柱关联**：指标与日志通过 Grafana + Loki 集成，日志与链路通过 traceId 字段关联，告警通知附带 Grafana 面板链接，实现"指标发现 → 链路定位 → 日志定因"的穿透式排障。
> 可观测性详细设计详见 [02_可观测性设计.md](技术到场景/02_可观测性设计.md)，数据架构详见 [06_数据架构与大数据处理.md](技术到场景/06_数据架构与大数据处理.md)，持续交付与自动化运维详见 [03_持续交付与自动化运维.md](技术到场景/03_持续交付与自动化运维.md)。

## 四、权限与访问控制

```mermaid
flowchart LR
    A["用户登录"] --> B["统一认证中心"]
    B --> C["JWT令牌"]
    C --> D["API网关"]
    D --> E["权限服务"]
    D --> F["监控服务"]
    D --> G["告警服务"]
    D --> H["工单服务"]
    E --> I["RBAC角色控制"]
    E --> J["数据域隔离"]
    F --> K["审计日志"]
    G --> K
    H --> K
```

## 五、基础设施与高可用

```mermaid
flowchart TD
    subgraph 流量入口
        A["边缘节点 24 台"]
        U["WEB/移动端用户"]
        B["内网DNS"]
        V["VIP"]
        A --> B
        U --> B
        B --> V
    end

    subgraph 负载均衡层
        LV["LVS（DR）集群"]
        NX["Nginx/Keepalived"]
        V --> LV
        LV --> NX
    end

    subgraph 应用层
        GW["API网关集群"]
        SVC["Kubernetes多副本服务"]
        NX --> GW
        GW --> SVC
    end

    subgraph 存储与监控
        REDIS["Redis Sentinel"]
        MYSQL["MySQL主从/MGR"]
        MON["Prometheus + Grafana"]
        BACKUP["备份恢复系统"]
        CACHE["边缘本地缓存"]
        SVC --> REDIS
        SVC --> MYSQL
        SVC --> MON
        MYSQL --> BACKUP
        A --> CACHE
    end
```

## 六、持续交付

```mermaid
flowchart LR
    A["代码提交"] --> B["CI流水线<br/>单元测试/代码扫描"]
    B --> C["镜像构建"]
    C --> D["镜像仓库"]
    D --> E["容器编排平台"]
    E --> F["滚动升级/灰度发布"]
    F --> G["监控与回滚"]
```

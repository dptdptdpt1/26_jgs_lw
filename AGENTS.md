# 工作区协作说明

## 基本口径

- `项目固定背景.md` 是项目参数和统一口径的唯一来源，优先级最高。
- `项目图示.md` 是图示唯一维护处；`附录/` 只放跨主题公共口径。
- `论文真题汇编.md` 定主题，`论文主题素材/` 供正文取材，`论文模版.md` 负责组装。

## 写作规则

- 默认围绕固定项目写，不另起项目。
- 站在系统架构师视角写，不写成产品介绍。
- 使用“问题识别 -> 方案选择 -> 技术落地 -> 效果与取舍”的论证结构。
- 技术名词必须服务于项目问题，避免堆砌。
- 尽量少写具体编程语言，优先写架构模式、协议标准、中间件、平台组件和工程治理能力。
- 区分项目当前方案和考试可延展拔高点。

## 题目处理流程

1. 查 `论文真题汇编.md`，判断题目所属主题。
2. 从 `论文主题素材/` 选择 2 到 3 个素材文件。
3. 用 `论文模版.md` 组装摘要、背景、主体和结论。
4. 重写主体内容，不能机械拼接。
5. 检查项目参数、架构师视角、题目扣合和取舍表达。

## 文件维护规则

- 说明性文件保持短，避免解释性废话。
- 一个信息只放在一个最合适的位置，其他文件只引用。
- 新增资料保持 Markdown 层级清晰，文件名继续使用中文命名。
- 不覆盖用户已有未提交变更；如文件已有改动，先读取并增量修改。
